<!doctype html>
<html>
  <head>
    {{ content_for_header }}

    {{ 'my_css.css' | asset_url | stylesheet_tag }}

    {{ 'my_js.js' | asset_url | script_tag }}

    {% if settings.use_font == true %}
      {{ 'my_font.css' | asset_url | stylesheet_tag }}
    {% endif %}
  </head>

  <body>
    <div class="my_font my_layout">

      {% sections 'header_group' %}

      {{ content_for_layout }}

      {% sections 'footer_group' %}
      
    </div>
  </body>
</html>
