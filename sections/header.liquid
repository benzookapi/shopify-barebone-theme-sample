<div class="my_section my_header">
  <div class="my_block_title">Section</div>

  <h1>{{ section.settings.title }}</h1>

  <p style="text-align: right; margin-right: 1em;">
    👤 <a href="">My page</a>&nbsp;&nbsp;&nbsp;&nbsp; 🛒 <a href="">My cart</a>&nbsp;&nbsp;&nbsp;&nbsp;

    <select onchange="changeLang(this);">
      <option value="">Choose your language</option>
      {% for locale in localization.available_languages %}
        <option value="{{ locale.root_url }}">{{ locale.name }}</option>
      {% endfor %}
    </select>
  </p>

  {% for block in section.blocks %}
    <div class="my_block">
      <div class="my_block_title">Block ({% increment i %})</div>

      {% case block.type %}
        {% when 'my_type_1' %}
          <div>{{ block.settings.my_logo | img_url: 'medium' | img_tag }}</div>
        {% when 'my_type_2' %}
          <div style="background-color: {{ block.settings.my_color }};">&nbsp;</div>
      {% endcase %}
    </div>
  {% endfor %}

  <p>👉 <a href="https://shopify.dev/docs/themes/architecture/layouts" target="_blank">Dev. doc</a></p>
</div>

{% schema %}
{
  "name": "t:sections.header.name",
  "limit": 1,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Header Title",
      "default": "THIS IS MY HEADER"
    }
  ],
  "max_blocks": 3,
  "blocks": [
    {
      "name": "My Logo",
      "type": "my_type_1",
      "settings": [
        {
          "type": "image_picker",
          "id": "my_logo",
          "label": "My Logo"
        }
      ]
    },
    {
      "name": "My Color",
      "type": "my_type_2",
      "settings": [
        {
          "type": "color",
          "id": "my_color",
          "label": "My Color"
        }
      ]
    }
  ],
  "enabled_on": {
    "groups": ["header"]
  }
}
{% endschema %}
