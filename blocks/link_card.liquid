{% doc %}
  Renders a reusable URL link card as a theme block.

  @example
  {% content_for 'block', type: 'link_card', id: 'link-card' %}
{% enddoc %}

{% if block.settings.url != blank %}
  <a class="my-theme-block-card" href="{{ block.settings.url }}" {{ block.shopify_attributes }}>
    <span class="my-theme-block-card__label">{{ block.settings.label | default: block.settings.url }}</span>
    <span class="my-theme-block-card__meta">{{ block.settings.url }}</span>
  </a>
{% else %}
  <div class="my-theme-block-card my-theme-block-card--empty" {{ block.shopify_attributes }}>
    <span class="my-theme-block-card__label">Choose a URL</span>
  </div>
{% endif %}

{% stylesheet %}
  .my-theme-block-card {
    display: block;
    border: 1px solid #d8d8d8;
    padding: 1rem;
    text-decoration: none;
    color: inherit;
    background: #fff;
  }

  .my-theme-block-card__label {
    display: block;
    font-weight: 700;
  }

  .my-theme-block-card__meta {
    display: block;
    margin-top: 0.35rem;
    color: #666;
    font-size: 0.9rem;
  }

  .my-theme-block-card--empty {
    color: #666;
  }
{% endstylesheet %}

{% schema %}
{
    "name": "Link card",
    "settings": [
        {
            "type": "url",
            "id": "url",
            "label": "URL",
            "default": "/collections"
        },
        {
            "type": "text",
            "id": "label",
            "label": "Label"
        }
    ],
    "presets": [
        {
            "name": "Link card"
        }
    ]
}
{% endschema %}
