{% doc %}
  Renders a reusable product link card as a theme block.

  @example
  {% content_for 'block', type: 'product_card', id: 'product-card' %}
{% enddoc %}

{% if block.settings.product != blank %}
  <a class="my-theme-block-card" href="{{ block.settings.product.url }}" {{ block.shopify_attributes }}>
    <span class="my-theme-block-card__label">{{ block.settings.label | default: block.settings.product.title }}</span>
    <span class="my-theme-block-card__meta">{{ block.settings.product.price | money_with_currency }}</span>
  </a>
{% else %}
  <div class="my-theme-block-card my-theme-block-card--empty" {{ block.shopify_attributes }}>
    <span class="my-theme-block-card__label">Choose a product</span>
  </div>
{% endif %}

{% stylesheet %}
  .my-theme-block-card {
    display: block;
    border: 1px solid #d8d8d8;
    padding: 1rem;
    text-decoration: none;
    color: inherit;
    background: #fff;
  }

  .my-theme-block-card__label {
    display: block;
    font-weight: 700;
  }

  .my-theme-block-card__meta {
    display: block;
    margin-top: 0.35rem;
    color: #666;
    font-size: 0.9rem;
  }

  .my-theme-block-card--empty {
    color: #666;
  }
{% endstylesheet %}

{% schema %}
{
    "name": "Product card",
    "settings": [
        {
            "type": "product",
            "id": "product",
            "label": "Product"
        },
        {
            "type": "text",
            "id": "label",
            "label": "Custom label"
        }
    ],
    "presets": [
        {
            "name": "Product card"
        }
    ]
}
{% endschema %}
